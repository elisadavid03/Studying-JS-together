1. Introduced in ES6, template literals allow embedding expressions and multi-line strings using backticks (``).
   1. Example:
      ```javascript
      let name = 'Alice';

      let greeting = `Hello, ${name}!`;

      console.log(greeting);  // Output: 'Hello, Alice!'
